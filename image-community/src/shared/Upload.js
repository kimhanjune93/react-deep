import React from "react";
import Rect from "react";

const Upload = () => {
    return(
        <React.Fragment>
            <input type="file"/>
        </React.Fragment>
    );
}

export default Upload;